# Netlify configuration for νόησις workspace

[build]
  # Publish from workspace root (where index.html lives)
  publish = "."
  
  # No build needed - chat app uses import maps and loads from esm.sh CDN
  command = ""

# SPA-style routing for Chat app
[[redirects]]
  from = "/chat/*"
  to = "/chat/index.html"
  status = 200

# Fallback to root index.html for other routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers
[[headers]]
  for = "/*"
    [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
